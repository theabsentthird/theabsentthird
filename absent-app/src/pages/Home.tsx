import React from 'react';
import MapView from '../components/MapView';

const Home = () => {
  return (
    <main className="w-full h-screen">
      <MapView />
    </main>
  );
};

export default Home;
